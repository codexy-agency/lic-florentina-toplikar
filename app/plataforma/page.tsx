export const dynamic = "force-dynamic";

export default function PlataformaPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-24">
      <p className="cx-rotulo">Codexy</p>
      <h1 className="mt-4 text-3xl font-semibold tracking-tight">Landing en construcción</h1>
      <p className="mt-3 text-[15px] leading-relaxed" style={{ color: "var(--apagado)" }}>
        Esta ruta ya está enganchada al dominio de la plataforma. El contenido llega enseguida.
      </p>
    </main>
  );
}
